<h1>Edit <%= @kitten.name %></h1>

<%= form_with(model: @kitten, local: true, method: :patch) do |f| %>
    <%= render 'form', f: f %>  
<% end %>

<%= button_to 'Delete Kitten', @kitten, 
        method: :delete, 
        data: { confirm: 'Are you sure?' }, 
        class: 'btn btn-danger' %>